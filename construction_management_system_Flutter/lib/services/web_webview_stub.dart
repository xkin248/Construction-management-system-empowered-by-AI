// Stub used on every platform except web (Android/iOS/desktop). On those
// platforms webview_flutter_android / webview_flutter_wkwebview register
// themselves automatically, so there's nothing to do here.
void registerWebWebView() {}
